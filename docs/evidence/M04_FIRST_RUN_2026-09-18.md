# M04 First macOS Run — 2026-09-18

Mission: **M04 — Logos Storage CID round-trip proof**

Status: `partial-pass / runner-readiness-race-fixed / rerun-pending`

## What passed

The first macOS Apple Silicon M04 run verified the M03 input before Storage work:

- input byte length: `139733363`
- expected MD5: `426bd510159627dc139d4d0ad3bc6acd`
- observed MD5: `426bd510159627dc139d4d0ad3bc6acd`

Build/install also passed:

- `logoscore` built
- `lgpm` built
- pinned `logos-co/logos-storage-module@bcc29f05e2a7b4a2d215d09640aaa7436359906b` built
- Storage LGX installed successfully
- installed module reported:
  - name: `storage_module`
  - version: `2.1.3`
  - type: `core`
  - category: `protocol`

## Where the run stopped

Immediately after launching `logoscore`, the runner waited a fixed three seconds and called:

`logoscore --config-dir <isolated-dir> status`

The client returned:

`{"daemon":{"status":"not_configured"}}`

Because the script uses `set -e`, this non-ready status aborted the proof before module loading, Storage node initialization, upload, CID creation, or download.

No CID, Storage manifest, or retrieved verification file existed after the run.

## Interpretation

Current `logoscore` creates its live daemon state and local client config only after transports have bound. A fixed three-second delay is therefore not a valid readiness condition.

This run does **not** show a Storage module build/install failure.

## Runner correction

The M04 script now:

1. pins `logoscore` to the exact revision observed in this run:
   `ab1ae3509070163c949afdc30f5d5db8898fd5b0`;
2. polls the daemon process for up to 90 seconds;
3. waits for both:
   - `daemon/state.json`
   - `client/config.json`;
4. requires a successful `status` before continuing;
5. dumps config files/status/daemon log if readiness times out;
6. uses the pinned Logos Storage flake's declared public Logos Nix cache via
   `--accept-flake-config` to avoid rebuilding unnecessary dependencies.

## Truth

- M03 input integrity: `locally-verified`
- Storage module build/install: `locally-verified`
- Storage node start: not yet verified
- Storage upload/CID: not yet verified
- Storage download/byte round-trip: not yet verified

## Next

Pull the corrected M04 branch and rerun `scripts/m04-local-storage-proof.sh`.
