# M03 Live Geofabrik Proof — macOS Apple Silicon — 2026-09-18

Mission: **M03 — Geofabrik discovery + checksum proof**

Status: `partial-pass-source-mapping-fix-required`

## Environment

- Platform: macOS Apple Silicon development machine
- Repository: `Devpen787/logos-osm-distribution`
- Branch: `feat/m03-geofabrik-discovery`
- Proof lane: live Geofabrik source + real PBF bytes

## Local Rust verification

The workspace compiled successfully and all local tests passed:

- `geofabrik`: 6 passed, 0 failed
- `osm-domain`: 3 passed, 0 failed

Covered behavior includes:

- fail-closed checksum mismatch;
- MD5 manifest parsing;
- streamed byte hashing;
- LP-0018 parent normalization;
- source-parent disambiguation for `us/georgia`;
- frozen-set structural validation.

## Live Geofabrik index

Current index downloaded from:

`https://download.geofabrik.de/index-v1-nogeom.json`

Ethiopia resolved correctly:

- canonical id: `ethiopia`
- source id: `ethiopia`
- registry parent: `null`
- source parent: `africa`
- level: `country`
- PBF: `https://download.geofabrik.de/africa/ethiopia-latest.osm.pbf`
- MD5: `https://download.geofabrik.de/africa/ethiopia-latest.osm.pbf.md5`

### Frozen-set audit result

The first live `resolve-all` attempt stopped at:

`LP-0018 region ireland is missing from Geofabrik index (source id=ireland, parent=europe)`

Root cause: current Geofabrik publishes the relevant country-level extract as `ireland-and-northern-ireland`, parent `europe`.

This is a source-mapping defect in the implementation, not a network/download failure.

Patch: canonical/source leaf updated to `ireland-and-northern-ireland` with a regression test. The full live 72-region audit must be rerun before F-01 is marked locally verified.

## Real Ethiopia snapshot integrity proof

Published Geofabrik MD5:

`426bd510159627dc139d4d0ad3bc6acd  ethiopia-latest.osm.pbf`

Downloaded PBF byte length:

`139733363`

Verification result:

- expected MD5: `426bd510159627dc139d4d0ad3bc6acd`
- observed MD5: `426bd510159627dc139d4d0ad3bc6acd`
- byte length: `139733363`
- filename: `ethiopia-latest.osm.pbf`

Result: **byte-integrity proof passed**.

## Source/version evidence

Observed response metadata for the real PBF request included:

- `Last-Modified: Thu, 17 Sep 2026 22:50:17 GMT`
- `ETag: "8542973-65bb59b94857b"`
- final content length: `139733363`

The captured headers include an earlier small response in the redirect chain, so version extraction code must target the final PBF response rather than blindly taking the first `Content-Length`.

## PBF snapshot metadata

Decoded from the PBF header:

- replication timestamp: `1789676465` = `2026-09-17T20:21:05Z`
- replication sequence number: `4905`
- replication base URL: `https://download.geofabrik.de/africa/ethiopia-updates`
- writing program: `osmium/1.16.0`
- source: unset

## Truth labels

- Workspace tests: `locally-verified`
- Ethiopia live region resolution: `locally-verified`
- Ethiopia real-byte MD5 integrity: `locally-verified`
- Ethiopia PBF metadata extraction: `locally-verified`
- Full 72-region current-index resolution: `blocked` at time of first run by Ireland mapping; patch applied, rerun required
- Logos Storage / LEZ / testnet claims: not part of M03

## Next

1. Verify CI after the Ireland mapping patch.
2. Rerun only the live index audit (`resolve-all`); the 133 MB Ethiopia PBF does **not** need to be downloaded again.
3. If 72/72 resolves, update F-01 to locally verified and close M03.
